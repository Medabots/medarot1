Files in this directory are not directly built, they are instead referenced by symlinks in the ../kabuto and ../kuwagata folders.

These files have dependencies on a variable set during assembly (GAMEVERSION).